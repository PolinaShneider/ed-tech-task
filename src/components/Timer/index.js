import React from 'react';

import './styles.css';

const Timer = () => {
    return (
        <div className="Timer">
            <span className="Timer_digits">02:30</span>
        </div>
    );
};

export default Timer;
